/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nickjhathaway/elucidatornf Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Default config options for all compute environments
----------------------------------------------------------------------------------------
*/

// Global default params, used in configs
params {

    //project wide setting
    empty_file_fnp = "${projectDir}/etc/EMPTY_FILE.txt"

    // PathWeaver params
    pw_ncpus = 4                  // Number of extract region threads
    pw_pop_clustering_ncpus = 10  // Number of population clustering threads
    pw_seqs_table = null
    pw_seqs_table_seqs_col = null
    pw_seqs_table_name_col = null
    pw_seqs_table_target_col = null
    pw_gene_ids = null
    pw_samples_file = null
    pw_bed_fnp = null
    pw_extract_region_assemblies_extra_args = null
    genome_fnp = null
    bams_dir = null
    bams_file_ending = ".sorted.bam"
    meta_fnp = params.empty_file_fnp //sample meta
    render_pw_report = false
    pw_extract_region_assemblies_extra_args = null
    pw_pop_clustering_extra_args = null
    pw_overwrite_dir = false

    //pw additional options
    run_sub_segments_determination = false
    sub_segments_correction_occurence_cut_off = 5
    sub_segments_low_freq_cut_off = 5
    sub_segments_uniqueHapCountCutOff = 1
    sub_segments_extra_args = null

    // variant calling options
    do_variant_calling = false
    variant_calling_ncpus = 10
    known_amino_acid_changes_fnp = "${projectDir}/etc/EMPTY_FILE.txt"
    vc_variant_frequency_cut_off = 0.0099
    vc_variant_occurrence_cut_off = 2
    vc_no_pairwise_comps = false
    vc_primary_genome = false
    vc_correct_small_homopolymer_errors = null;
    meta_fields_to_calc_pop_diffs = "" //region,country,subRegion,secondaryRegion
    vc_extra_args = ""

    //illumina clustering
    illumina_clustering_paired_end_length = null
    illumina_clustering_error_profile = "illumina"
    illumina_clustering_min_sample_read_count = 50
    illumina_clustering_population_ncpus = 2
    illumina_clustering_trim_front = 1
    illumina_clustering_trim_back = 1

    //nanopore clustering
    nanopore_clustering_min_len = 100
    nanopore_clustering_min_sample_read_count = 10
    nanopore_primers_errors_allowed = 2
    nanopore_clustering_ncpus = 2
    nanopore_clustering_trim_front = 1
    nanopore_clustering_trim_back = 1
    nanopore_clustering_max_reads_use = null
    nanopore_extractor_use_inner_primers = false
    nanopore_clustering_lower_base = null //remove, upper are the options
    nanopore_extractor_primer_within_start = null //500
    nanopore_rename_key_fnp = null // a renmaing file, first column is the sample name e.g. barcode01 and the second column is a new sample name
    nanopore_render_clustering_report = false
    amplicon_clustering_pop_clustering_extra_args = "--removeOneSampOnlyOneOffHaps --excludeCommonlyLowFreqHaplotypes --excludeLowFreqOneOffs --rescueExcludedOneOffLowFreqHaplotypes"
    amplicon_clustering_test_number = null
    amplicon_clustering_number_of_files_to_investigate = null
    input_fastq_dir = null
    primers_fnp = null
    genome_dir = null
    gff_dir = null

    // WGS processing
    wgs_samples_keep_file = null
    wgs_rename_tsv = null
    wgs_skip_existing = true
    wgs_host1_filter_genome_fasta_fnp = null
    wgs_host1_filter_contigs_fnp = null
    wgs_host2_filter_genome_fasta_fnp = null
    wgs_host2_filter_contigs_fnp = null
    wgs_final_genome_fasta_fnp = null
    wgs_host_filter_any_mate = true
    wgs_host_filter_filter_with_unmapped_mate = true
    wgs_host_filter_min_mapq = 0

    // RNA seq
    rnaseq_host1_filter_genome_fasta_fnp = null
    rnaseq_host1_filter_contigs_fnp = null
    rnaseq_host2_filter_genome_fasta_fnp = null
    rnaseq_host2_filter_contigs_fnp = null
    rnaseq_salmon_index = null
    single_cell_seurat_r_object_fnp = null

    // Boilerplate options
    outdir                       = null
    publish_dir_mode             = 'copy'
    monochrome_logs              = false

    help                         = false
    help_full                    = false
    show_hidden                  = false
    version                      = false
    // pipelines_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/'

    // Config options
    config_profile_name        = null
    config_profile_description = null

    custom_config_version      = 'master'
    // custom_config_base         = "https://raw.githubusercontent.com/nf-core/configs/${params.custom_config_version}"
    config_profile_contact     = null
    config_profile_url         = null

    // Max Job Request Parameters
    //
    // Detected limits of the current host. Used to cap the ceilings below so a
    // request can never exceed this machine. (The VS Code extension may show a
    // false-positive on the `nextflow.util.*` reference; `nextflow lint` passes.)
    machine_cpus   = nextflow.util.SysHelper.getAvailCpus()
    machine_memory = nextflow.util.SysHelper.getAvailMemory()

    // Per-task resource ceilings used by `resourceLimits` to clamp each task's
    // request (not executor-wide limits). Override on the CLI (--max_task_cpus,
    // --max_task_memory, --max_task_time) or in a cluster profile. Effective
    // per-task ceiling = min(max_task_*, max_executor_*, machine_*).
    max_task_cpus   = 16
    max_task_memory = 128.GB
    max_task_time   = 72.h

    // Optional executor-wide ceilings (whole pipeline / global pool, local
    // executor). Cap how much of the machine Nextflow may use at once, e.g.
    // only 10 of 20 cpus. null = use all available; a set value is itself
    // capped to the machine. (For Slurm, throttle with executor.queueSize.)
    max_executor_cpus   = null
    max_executor_memory = null

    // Automatic retries (with scaled resources) per failing task. 0 disables.
    max_retry = 3

    // Back-compat values consumed by workflow code and cluster profiles:
    //   max_cpus - cpus a tool may use, capped to the per-task ceiling + machine
    //   max_time - time ceiling referenced by the SGE/SLURM profiles
    max_cpus = [ params.max_task_cpus as int, params.machine_cpus as int ].min()
    max_time = params.max_task_time

    // Computed once at config parse time, reused across all report files.
    trace_timestamp = new java.util.Date().format('yyyy-MM-dd_HH-mm-ss')

    // Schema validation default options
    validate_params            = true
}

// Load base.config by default for all pipelines
includeConfig 'conf/base.config'

// load profiles
includeConfig "conf/profiles.config"

// Load nf-core custom profiles from different Institutions
// includeConfig !System.getenv('NXF_OFFLINE') && params.custom_config_base ? "${params.custom_config_base}/nfcore_custom.config" : "/dev/null"

// Load nickjhathaway/elucidatornf custom profiles from different institutions.
// TODO nf-core: Optionally, you can add a pipeline-specific nf-core config at https://github.com/nf-core/configs
// includeConfig !System.getenv('NXF_OFFLINE') && params.custom_config_base ? "${params.custom_config_base}/pipeline/elucidatornf.config" : "/dev/null"

// Set default registry for Apptainer, Docker, Podman, Charliecloud and Singularity independent of -profile
// Will not be used unless Apptainer / Docker / Podman / Charliecloud / Singularity are enabled
// Set to your registry if you have a mirror of containers
// apptainer.registry    = 'quay.io'
// docker.registry       = 'quay.io'
// podman.registry       = 'quay.io'
// singularity.registry  = 'quay.io'
// charliecloud.registry = 'quay.io'

// apptainer.registry    = 'docker://'
// docker.registry       = 'docker://'
// singularity.registry  = 'docker://'




// Export these variables to prevent local Python/R libraries from conflicting with those in the container
// The JULIA depot path has been adjusted to a fixed path `/usr/local/share/julia` that needs to be used for packages in the container.
// See https://apeltzer.github.io/post/03-julia-lang-nextflow/ for details on that. Once we have a common agreement on where to keep Julia packages, this is adjustable.

env {
    PYTHONNOUSERSITE = 1
    R_PROFILE_USER   = "/.Rprofile"
    R_ENVIRON_USER   = "/.Renviron"
    JULIA_DEPOT_PATH = "/usr/local/share/julia"
}

  // Set bash options
process.shell = [
    "bash",
    "-C",         // No clobber - prevent output redirection from overwriting files.
    "-e",         // Exit if a tool returns a non-zero status/exit code
    "-u",         // Treat unset variables and parameters as an error
    "-o",         // Returns the status of the last command to exit..
    "pipefail"    //   ..with a non-zero status or zero if all successfully execute
]

// Disable process selector warnings by default. Use debug profile to enable warnings.
nextflow.enable.configProcessNamesValidation = false

timeline {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_timeline_${params.trace_timestamp}.html"
}
report {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_report_${params.trace_timestamp}.html"
}
trace {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_trace_${params.trace_timestamp}.txt"
}
dag {
    enabled = true
    file    = "${params.outdir}/pipeline_info/pipeline_dag_${params.trace_timestamp}.html"
}

manifest {
    name            = 'nickjhathaway/elucidatornf'
    author          = """Nicholas Hathaway"""
    homePage        = 'https://github.com/nickjhathaway/elucidatornf'
    description     = """A collection of workflows to stream together tools from within elucidator"""
    mainScript      = 'main.nf'
    nextflowVersion = '!>=24.04.2'
    version         = '1.0.0dev'
    doi             = ''
}

// Nextflow plugins
plugins {
    id 'nf-schema@2.1.1' // Validation of pipeline parameters and creation of an input channel from a sample sheet
}

// validation {
//     defaultIgnoreParams = ["genomes"]
//     help {
//         enabled = true
//         command = "nextflow run nickjhathaway/elucidatornf -profile <docker/singularity/.../institute> --input samplesheet.csv --outdir <OUTDIR>"
//         fullParameter = "help_full"
//         showHiddenParameter = "show_hidden"

//     }
// }

// Load modules.config for DSL2 module specific options
includeConfig 'conf/modules.config'


